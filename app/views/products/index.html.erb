
<h1>Produtos</h1>
<% @products.each do |product| %>
  <p>
  <%= product.name %>
  <%= product.brand %>
  <%= product.description %>
  <%= product.price %>
  <%= link_to "Ver produto", product_path(product) %>
  </p>
<% end  %>


<%= link_to "Home", root_path %>

